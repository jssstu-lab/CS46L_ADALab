#include <stdio.h>

int search_linear(int A[], int n, int key) {
	for (int i = 0; i < n; i++) {
		if (A[i] == key)
			return key;
	return -1;
}

int search_binary(int A[], int n, int key) {
	
}

int main() {
	return 0;
}
